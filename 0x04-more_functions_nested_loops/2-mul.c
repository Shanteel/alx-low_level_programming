#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first to multiply
 * @b: multiplied by the first
 * Return: multiplication result between parameters a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
